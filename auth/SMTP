$mail = new PHPMailer(true);

try {

    $mail->isSMTP();
    $mail->Host = 'smtp.gmail.com';
    $mail->SMTPAuth = true;

    $mail->Username = 'snehaldeshmukh10112007@gmail.com';
    $mail->Password = 'jfrz rlwm uxha jtds';

    $mail->SMTPSecure = PHPMailer::ENCRYPTION_STARTTLS;
    $mail->Port = 587;

    $mail->setFrom('snehaldeshmukh10112007@gmail.com', 'AI Resume Analyzer');
    $mail->addAddress($email, $user['fullname']);

    $mail->isHTML(true);
    $mail->Subject = 'Reset Password';
    $mail->Body = 'Your email body here...';

    $mail->SMTPDebug = 2;
$mail->Debugoutput = 'html';
    $mail->send();

} catch (Exception $e) {
    echo "Email could not be sent.<br>";
    echo $mail->ErrorInfo;
}